component:
  name: pw-appx-shew-support
  type: service
  configuration:
    runtime: python3.11  # Choreo will use Python 3.11 as the runtime
    build: auto  # Automatically handles the build for the selected runtime
  endpoints:
    - name: dummy
      type: http
      port: 8080
      public: false
  environment:
    - name: NODE_ENV
      value: production
